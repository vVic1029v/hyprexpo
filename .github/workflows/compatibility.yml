name: Compatibility

on:
  pull_request:
    branches: [master, hyprland-git, exp/**]
  push:
    branches: [master, hyprland-git, exp/**]
  workflow_dispatch:
    inputs:
      target_branch:
        description: Compatibility contract to test on the selected workflow ref
        required: true
        default: master
        type: choice
        options: [master, hyprland-git]

permissions:
  contents: read

concurrency:
  group: compatibility-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  targets:
    name: Resolve compatibility targets
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.resolve.outputs.matrix }}
      contract: ${{ steps.resolve.outputs.contract }}
      gate: ${{ steps.resolve.outputs.gate }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - id: resolve
        env:
          TARGET_BRANCH: ${{ github.base_ref || inputs.target_branch || github.ref_name }}
          PR_NUMBER: ${{ github.event.pull_request.number || 0 }}
          PR_DRAFT: ${{ github.event.pull_request.draft || false }}
          PR_HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          PR_HEAD: ${{ github.head_ref || github.ref_name }}
        run: |
          descriptor=$(python3 scripts/ci-targets.py contract \
            --repository "$GITHUB_REPOSITORY" --number "$PR_NUMBER" \
            --base "$TARGET_BRANCH" --head-repository "$PR_HEAD_REPOSITORY" \
            --head "$PR_HEAD" --draft "$PR_DRAFT")
          CONTRACT="$descriptor" python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          import os
          import subprocess
          descriptor = json.loads(os.environ["CONTRACT"])
          matrix = subprocess.check_output(["python3", "scripts/ci-targets.py", "matrix", descriptor["track"]], text=True).strip()
          print(f"contract={descriptor['track']}")
          print(f"gate={descriptor['gate']}")
          print(f"matrix={matrix}")
          PY

  changes:
    name: Classify compatibility build need
    runs-on: ubuntu-24.04
    outputs:
      build_needed: ${{ steps.classify.outputs.build_needed }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - id: classify
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          if [[ "${GITHUB_EVENT_NAME}" = workflow_dispatch ]] \
            || [[ ! "$BASE_SHA" =~ ^[0-9a-f]{40}$ ]] \
            || [[ ! "$HEAD_SHA" =~ ^[0-9a-f]{40}$ ]] \
            || ! git cat-file -e "${BASE_SHA}^{commit}" 2>/dev/null; then
            echo 'build_needed=true' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "build_needed=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" | python3 scripts/ci-targets.py build-needed)" >> "$GITHUB_OUTPUT"

  regressions:
    name: Regression tests
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Verify tooling contracts and negative cases
        run: make test-tooling
      - name: Compile and run all standalone regression suites
        run: make -B test
      - name: Verify every historical release pin is reachable
        run: make check-pins REF=HEAD

  build:
    name: Build / ${{ matrix.name }}
    needs: [targets, changes]
    if: needs.changes.outputs.build_needed == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.targets.outputs.matrix) }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            sandbox = true
      - name: Build exact source and record dependency / generated-header provenance
        env:
          BUNNY_CACHE_URL: ${{ vars.BUNNY_CACHE_URL }}
          BUNNY_CACHE_PUBLIC_KEY: ${{ vars.BUNNY_CACHE_PUBLIC_KEY }}
          BUNNY_CACHE_PREFIX: ${{ vars.BUNNY_CACHE_PREFIX }}
          BUNNY_CACHE_STORAGE_ENDPOINT: ${{ vars.BUNNY_CACHE_STORAGE_ENDPOINT }}
          BUNNY_CACHE_WRITE: ${{ github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/hyprland-git') && vars.BUNNY_CACHE_URL != '' }}
          BUNNY_CACHE_SIGNING_KEY: ${{ github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/hyprland-git') && secrets.BUNNY_CACHE_SIGNING_KEY || '' }}
          BUNNY_CACHE_STORAGE_ZONE: ${{ github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/hyprland-git') && secrets.BUNNY_CACHE_STORAGE_ZONE || '' }}
          BUNNY_CACHE_STORAGE_PASSWORD: ${{ github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/hyprland-git') && secrets.BUNNY_CACHE_STORAGE_PASSWORD || '' }}
          UPSTREAM_REV: ${{ matrix.rev }}
          CI_TARGET_BRANCH: ${{ needs.targets.outputs.contract }}
          EVIDENCE_DIR: ${{ runner.temp }}/evidence-${{ matrix.name }}
        run: ./scripts/ci-build.sh "$UPSTREAM_REV" "$EVIDENCE_DIR"
      - name: Preserve artifacts and failure logs
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: compatibility-${{ matrix.name }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/evidence-${{ matrix.name }}
          if-no-files-found: error

  gate:
    # Never emit a skipped opposite-track gate: GitHub accepts skipped checks
    # for protection, including another workflow run on the same commit.
    name: ${{ needs.targets.outputs.gate }}
    if: always()
    needs: [targets, changes, regressions, build]
    runs-on: ubuntu-24.04
    steps:
      - env:
          TARGETS_RESULT: ${{ needs.targets.result }}
          CHANGES_RESULT: ${{ needs.changes.result }}
          TEST_RESULT: ${{ needs.regressions.result }}
          BUILD_RESULT: ${{ needs.build.result }}
        run: test "$TARGETS_RESULT" = success && test "$CHANGES_RESULT" = success && test "$TEST_RESULT" = success && { test "$BUILD_RESULT" = success || test "$BUILD_RESULT" = skipped; }
